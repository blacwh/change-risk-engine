{
  "name": "change-risk-engine",
  "version": "0.0.0",
  "private": true,
  "description": "Deterministic, explainable repository change-risk analysis.",
  "type": "module",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/blacwh/change-risk-engine.git"
  },
  "engines": {
    "node": "^20.19.0 || ^22.13.0 || >=24.0.0"
  },
  "packageManager": "npm@11.7.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "change-risk": "node apps/cli/dist/run.js",
    "format": "prettier --write \"**/*.{json,js,mjs,ts,yaml,yml}\"",
    "format:check": "prettier --check \"**/*.{json,js,mjs,ts,yaml,yml}\"",
    "lint": "eslint . --max-warnings 0",
    "package:action": "node scripts/package-action.mjs",
    "package:cli": "node scripts/package-cli.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "verify:package": "node scripts/verify-package.mjs",
    "verify:release": "node scripts/release-readiness.mjs",
    "quality": "npm run format:check && npm run lint && npm run typecheck && npm test && npm run build && npm run verify:action",
    "verify:action": "node scripts/package-action.mjs --check"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^20.19.43",
    "esbuild": "^0.25.12",
    "eslint": "^10.8.0",
    "prettier": "^3.9.6",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.65.0",
    "vitest": "^4.1.10"
  }
}
